# 创建zookeeper headless服务
apiVersion: v1
kind: Service
metadata:
  name: redisphp-hs
  namespace: {{ namespace }}
  labels:
    app: redisphp
spec:
  ports:
  - port: 6379
    name: server
  - port: 16379
    name: cluster
  clusterIP: None
  selector:
    app: redisphp