# 创建zookeeper headless服务
apiVersion: v1
kind: Service
metadata:
  name: rediserl-hs
  namespace: {{ namespace }}
  labels:
    app: rediserl
spec:
  ports:
  - port: 6379
    name: server
  - port: 16379
    name: cluster
  clusterIP: None
  selector:
    app: rediserl