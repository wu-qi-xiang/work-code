apiVersion: apps/v1
kind: Deployment
metadata:
  name: titan-connect-selector
  namespace: "{{namespace}}"
  labels:
    app: titan-connect-selector
spec:
  replicas: {{connectselector_replicas}}
  selector:
    matchLabels:
      app: titan-connect-selector
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  progressDeadlineSeconds: 120
  minReadySeconds: 30
  revisionHistoryLimit: 3
  template:
    metadata:
      name: titan-connect-selector
      labels:
        app: titan-connect-selector
    spec:
      nodeSelector:
        "{{connectselector_nodelabel}}": "true"
      serviceAccountName: "{{service_account}}"
      containers:
        - image: "{{connectselector_image}}"
          imagePullPolicy: "{{ connectselector_pullPolicy }}"
          name: titan-connect-selector
          resources:
            limits:
              cpu: "{{ connectselector_limit_cpu }}"
              memory: "{{ connectselector_limit_memory }}"
            requests:
              cpu: "{{cpu_req}}"
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /api/ping
              port: {{connectselector_port}}
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            exec:
              command:
                - "/bin/bash"
                - "-c"
                - result=`curl -k -sS http://127.0.0.1:{{connectselector_port}}/api/checkall 2>&1`; if [[ $result =~ 'abnormal_service":[]' ]]; then echo ok; else echo $result && exit 1; fi
            initialDelaySeconds: 45
            periodSeconds: 30
            timeoutSeconds: 15
            failureThreshold: 3
          command: ['/bin/sh', '-c', 'dockerize -secretdir /var/secrets/ -yaml-env /titan-env/titan-env.yml -template /titan-java-config/jaas_zk_client.conf:/data/app/titan-config/jaas_zk_client.conf -mergeToJson /titan-java-config/java.properties:/data/app/titan-config/java.json; chown -R titan:titan /data/app/titan-config; /data/app/titan-connect-selector/init.d/connect-selector start']
          ports:
            - containerPort: {{connectselector_port}}
              protocol: TCP
          volumeMounts:
            - name: var-secrets
              mountPath: /var/secrets/
            - name: titan-env
              mountPath: /titan-env
            - name: titan-java-config
              mountPath: /titan-java-config
            - mountPath: /data/titan-logs/java/connect-selector
              subPath: connect-selector
              name: titan-logs
            - mountPath: /data/app/titan-connect-selector/tomcat
              subPath: tomcat
              name: tmp
            - mountPath: /tmp
              name: tmp
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
      securityContext:
        fsGroup: {{connectselector_gid}}
      volumes:
        - name: var-secrets
          secret:
            secretName: titan-all-secrets
        - name: titan-env
          configMap:
            name: titan-env
        - name: titan-java-config
          configMap:
            name: titan-java-config
        - emptyDir: {}
          name: tmp
