# 创建zookeeper headless服务
apiVersion: v1
kind: Service
metadata:
  name: redisjava
  namespace: {{ namespace }}
  labels:
    app: redisjava
spec:
  ports:
  - port: 6379
    name: server
  selector:
    app: redisjava